# Estado inicial: el escritorio todavia no tiene contrasena, asi que Caddy
# manda todo a la pagina de alta. devbox-setup reescribe este archivo y recarga
# Caddy cuando la definis.
reverse_proxy 127.0.0.1:6902 {
    header_up X-Real-IP {remote_host}
}
